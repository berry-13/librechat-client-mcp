# GitHub Personal Access Token for higher API rate limits (60 req/hr without, 5000 req/hr with)
# Create one at: https://github.com/settings/tokens
GITHUB_PERSONAL_ACCESS_TOKEN=

# Transport mode: stdio | sse | dual
MCP_TRANSPORT_MODE=stdio

# SSE/HTTP transport host (default: 0.0.0.0)
MCP_HOST=0.0.0.0

# SSE/HTTP transport port (default: 7424)
MCP_PORT=7424

# CORS origins for SSE/HTTP transport (comma-separated, default: *)
MCP_CORS_ORIGINS=*

# Port override (used by Railway and other PaaS platforms)
# PORT=

# Node environment
NODE_ENV=development
